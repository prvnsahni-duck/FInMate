# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.angular

# Angular component templates use Angular-specific attribute syntax
# (e.g. [class.bg-error/15]) that Prettier's HTML parser cannot handle.
**/*.html
