import { CreateExpenseDto as SharedCreateExpenseDto } from '@finmate/data-models';

// Local skeleton DTO for backend module boundaries.
export class CreateExpenseDto extends SharedCreateExpenseDto {}
