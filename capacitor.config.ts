import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.finmate.app',
  appName: 'FinMate',
  webDir: 'dist/frontend/browser'
};

export default config;
