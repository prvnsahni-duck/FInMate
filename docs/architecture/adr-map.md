# ADR → Affected Files Map

Maps each architectural decision to the files it governs, so a task can read only the relevant files.

> **Note:** Numbered ADR files (ADR-000…ADR-015) are not yet committed to this repo. Until they are
> imported into `docs/architecture/`, the "Decision source" column points at the authoritative on-disk
> section. When ADRs are added, replace the source column with the ADR id and keep the file lists.

| Decision (source)                                                                                 | Governs                                                        | Affected files                                                                                                                                                                                                                                                                                |
| ------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **ZK model** (ARCHITECTURE §2; frozen-decisions "Security")                                       | Client-side encryption of titles/notes; backend never decrypts | `shared/.../expense.entity.ts`, `note.entity.ts`, `dto/is-ciphertext.decorator.ts`, all `dto/*.dto.ts` with ciphertext fields, `frontend/.../encryption.service.ts`, `expense-decryption.service.ts`                                                                                          |
| **Key Management Option 2** (PROJECT_DECISIONS; zk_group_key_provisioning_architecture.md)        | Versioned group keys, per-member wrapped keys, rotation        | `group-key-version.entity.ts`, `member-wrapped-group-key.entity.ts`, `groups.service.ts` (key methods), `groups.controller.ts` (keys/rotate), `dto/group-key.dto.ts`, `frontend/.../group-key.service.ts`, `zk-key-vault.service.ts`, migration `1719000000000-AddGroupKeyVersioningModel.ts` |
| **Amounts plaintext / no duplicate records** (PROJECT_DECISIONS; ARCHITECTURE Personal Dashboard) | Plaintext ledger columns; aggregation by join                  | `expense.entity.ts`, `expense-split.entity.ts`, `expenses.service.ts` (analytics), migrations `1718300…`/`1718600…`                                                                                                                                                                           |
| **Optimistic locking** (ARCHITECTURE §4.2)                                                        | `@VersionColumn` + `CON_VERSION_CONFLICT`                      | `expense.entity.ts`, `note.entity.ts`, `group.entity.ts`, `settlement.entity.ts`, `goal.entity.ts`, `recurring-expense.entity.ts`, `http-exception.filter.ts`, `frontend/.../optimistic-lock.interceptor.ts`, `automerge.service.ts`, `conflict-modal.service.ts`                             |
| **Soft delete + restore** (ARCHITECTURE §4.3)                                                     | `@DeleteDateColumn`, restore grace                             | `expense.entity.ts`, `expenses.service.ts` (delete/restore)                                                                                                                                                                                                                                   |
| **Currency consistency** (ARCHITECTURE §5)                                                        | Base currency lock; settlement currency match                  | `groups.service.ts` (currency lock), `settlements.service.ts`, `expenses.service.ts` (currency validation), `recurring-expenses.service.ts`                                                                                                                                                   |
| **Group archive / owner cannot leave / roles** (ARCHITECTURE §2.5)                                | Membership lifecycle + RBAC                                    | `groups.service.ts`, `members.controller.ts`, `auth/guards/group-roles.guard.ts`, `auth/decorators/group-roles.decorator.ts`, `group-member.entity.ts`                                                                                                                                        |
| **Auth & sessions** (ARCHITECTURE §2.4)                                                           | Dual JWT, Redis sessions, 2FA, throttling                      | `auth/*`, `guards/*`, `throttler/*`, `redis/redis.service.ts`, `main.ts` (helmet/cors), `dto/auth.dto.ts`                                                                                                                                                                                     |
| **Immutable audit log** (ARCHITECTURE §6)                                                         | Write-only audit + ipHash                                      | `audit-log.entity.ts`, `groups/services/groups-audit.service.ts`, audit writes in `expenses.service.ts`/`auth.service.ts`                                                                                                                                                                     |
| **AI opt-in** (PROJECT_DECISIONS)                                                                 | Server-enforced consent gate                                   | `ai/ai.controller.ts`, `ai.service.ts`, `user.entity.ts` (needs `ai_opt_in`), `frontend/.../dashboard`                                                                                                                                                                                        |
| **User deletion = PII only** (PROJECT_DECISIONS)                                                  | Account deletion semantics                                     | `users/users.controller.ts`, `users.service.ts` (unimplemented — PF-001)                                                                                                                                                                                                                      |
